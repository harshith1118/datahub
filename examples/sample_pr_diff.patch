--- a/mock_data/broken_dbt_model.sql
+++ b/mock_data/broken_dbt_model.sql
@@ -1,11 +1,11 @@
 -- broken_dbt_model.sql
 -- Simulates a dbt model that references an outdated column name.
--- The column `user_id` was renamed to `account_id` in the source schema.
+-- The column `account_id` was renamed to `account_id` in the source schema.
 
 WITH source AS (
     SELECT
         id,
-        user_id,  -- BUG: column renamed to account_id
+        account_id,  -- BUG: column renamed to account_id
         email,
         created_at
     FROM raw.users
@@ -14,7 +14,7 @@
 transformed AS (
     SELECT
         id,
-        user_id AS customer_id,  -- BUG: column renamed to account_id
+        account_id AS customer_id,  -- BUG: column renamed to account_id
         email,
         created_at
     FROM source
